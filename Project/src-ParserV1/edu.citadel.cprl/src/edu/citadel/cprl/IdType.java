package edu.citadel.cprl;

/**
 * The types for identifiers stored in the identifier table.
 */
public enum IdType
  {
    constantId,
    variableId,
    arrayTypeId,
    recordTypeId,
    stringTypeId,
    fieldId,
    procedureId,
    functionId;
  }
