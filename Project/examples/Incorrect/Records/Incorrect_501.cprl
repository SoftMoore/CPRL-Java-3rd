type Point = record
  {
    x : Integer;
    y : Integer;
  };

type T = record
  {
    x, y : Integer;   // each field must be declared separately
  };

proc main()
  {
    var p : Point;

    p.x := 5;
    p.y := 2         // missing semicolon
  }
