fun f1() : Integer
  {
    return 7;
  }

proc p1(x : Integer)
  {
    writeln x;
  }

proc main()
  {
    var x : Integer;
    p2();        // call to procedure that does not exist
    x := f2();   // call to function that does not exist
  }
