const n := 5;
var   x : Integer;

fun f(b : Boolean) : Integer
  {
    if b then
        return 1;
    else
        return -1;
  }

proc main()
  {
    x := f(n);       // incorrect parameter type
    write x;
  }
