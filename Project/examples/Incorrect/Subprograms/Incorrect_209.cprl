var x : Integer;

fun f(b : Boolean) : Integer
  {
    if b then
        return 1;
    else
        return -1;
  }

proc main()
  {
    x := f(true, 7);       // incorrect number of parameters
    writeln x;
  }
